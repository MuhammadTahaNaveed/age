name: lets see

on:
  pull_request:
    types: [labeled]

jobs:
  tweet:
    if: >-
        contains(github.event.pull_request.labels.*.name, 'auto-create')
    runs-on: ubuntu-latest
    steps:
      - name: print something
        run: echo "hello world"