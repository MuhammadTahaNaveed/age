name: lets see

on:
  pull_request:
    types: [labeled]

jobs:
  tweet:
    if: >-
        contains(github.event.pull_request.labels.*.name, 'PG11')
    runs-on: ubuntu-latest
    steps:
      - name: print something
        run: echo "hello world"